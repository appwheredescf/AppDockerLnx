node {
    def app
    stage('Checkout git repo') {
      git branch: 'master', url: params.git_repo
    }
        
    
    stage('Build image') {
        echo "estoy aqui"
        app = docker.build("jcendejas/appdockerlnx:dev", '"C:/Users/DELL/eclipse-workspace/AppDockerLnx" -f "C:/Users/DELL/eclipse-workspace/AppDockerLnx/AppDockerLnx/Dockerfile" ')
        echo "termine aqui aqui"
        
    }
    
    
    stage('Push image') {
    withCredentials([usernamePassword( credentialsId: 'DockerHUB', usernameVariable: 'USER', passwordVariable: 'PASSWORD')]) {
        def registry_url = "registry.hub.docker.com/"
        bat "docker login -u $USER -p $PASSWORD ${registry_url}"
        docker.withRegistry("http://${registry_url}", "DockerHUB") {
            // Push your image now
              bat "docker push jcendejas/appdockerlnx:dev"
            
     
        }
    }
        
     stage('run image') {
        echo "run images"
         bat "docker login -u jcendejas -p 136369Ctj"
        bat "docker run -p 6060:80 --name appdockerlnx -d jcendejas/appdockerlnx:dev"
        
    }
}
    




   
}
